%section.push
%section.container
  .page-header
    %h1 All Courses
    .pull-right
      = link_to new_admin_course_path, class: "btn btn-default btn-lg btn-tealeaf new_course_button" do
        %span.glyphicon.glyphicon-plus
        New Course
  .col-sm-10.courses-index
    %table.table
      %thead
        %tr
          %th(width="80%") Title
          %th(width="20%") Actions
      %tbody
        - @courses.each do |course|
          %tr
            %td
              %strong= link_to course.title, [:admin, course]
            %td
              = link_to "Edit", [:edit, :admin, course]
              - if course.can_be_published?
                = link_to "Publish", [:publish, :admin, course], method: :patch